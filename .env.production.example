# HMaigc 生产部署配置。复制为 .env.production，权限设为 600，禁止提交真实值。

# GitHub Actions 只上传版本化部署包；宿主机部署脚本使用本文件启动不可变镜像。
HMAIGC_IMAGE_REGISTRY=ghcr.io/你的GitHub用户名
HMAIGC_VERSION=vX.Y.Z
HMAIGC_BACKEND_IMAGE=ghcr.io/你的GitHub用户名/hmaigc-backend@sha256:替换为64位摘要
HMAIGC_WEB_IMAGE=ghcr.io/你的GitHub用户名/hmaigc-web@sha256:替换为64位摘要
BACKUP_HELPER_IMAGE=alpine@sha256:14358309a308569c32bdc37e2e0e9694be33a9d99e68afb0f5ff33cc1f695dce
HMAIGC_COMPOSE_PROJECT_NAME=hmaigc

# 发布状态、操作日志和恢复点必须在宿主机持久目录中，不依赖业务容器。
HMAIGC_DEPLOY_STATE_DIR=/var/lib/hmaigc-deploy

# 固定卷名是备份、恢复和回滚契约的一部分，生产启用后不得随意修改。
HMAIGC_BACKEND_DATA_VOLUME=hmaigc-backend-data
HMAIGC_POSTGRES_DATA_VOLUME=hmaigc-postgres-data
HMAIGC_REDIS_DATA_VOLUME=hmaigc-redis-data

POSTGRES_DB=hmaigc
POSTGRES_USER=hmaigc
POSTGRES_PASSWORD=请使用_openssl_rand_hex_32_生成

CANVAS_ENVIRONMENT=production
CANVAS_CORS_ORIGINS=https://你的域名
CANVAS_HTTP_HOST=127.0.0.1
CANVAS_HTTP_PORT=3000
CANVAS_REGISTRATION_ENABLED=false
CANVAS_ALLOW_PRIVATE_UPSTREAMS=false
CANVAS_ALLOWED_PRIVATE_UPSTREAM_HOSTS=
CANVAS_WORKER_CONCURRENCY=3
CANVAS_CHANNEL_CONCURRENCY=3

# 以下路径必须位于 HMAIGC_DEPLOY_STATE_DIR 之内。
HMAIGC_STATE_DIR=/var/lib/hmaigc-deploy/state
HMAIGC_BACKUP_DIR=/var/lib/hmaigc-deploy/backups
# 0 表示不自动删除；正整数表示保留最近 N 个恢复点，当前 release.env 引用的有效回滚点始终额外受保护。
HMAIGC_BACKUP_RETENTION=0
