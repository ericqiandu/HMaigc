# Domain Glossary

## 邀请关系（Referral Relationship）

新用户在首次注册时通过有效邀请码与邀请人建立的永久归属关系。一个受邀用户最多绑定一个邀请人，绑定后不可更换；自我邀请与注册完成后的补绑均不成立。关系状态只允许从待奖励进入已奖励或取消资格，原始绑定事实必须保留用于审计。

_Avoid_: 推广下线、渠道归属

## 首购邀请奖励（Referral First-Purchase Reward）

受邀用户首笔已支付的个人会员订单触发的一次性双边积分发放。会员订单、邀请人积分、受邀人积分与奖励记录必须在同一数据库事务内成功或失败；重复支付回调、重复核款、续费、退款后重购均不得再次发放。

_Avoid_: 注册奖励、推广佣金

## 邀请奖励规则（Referral Reward Rule）

运营方针对一个可售个人会员套餐配置的邀请人与受邀人积分金额。规则只影响尚未发生的首购奖励，历史奖励以实际发放记录为事实；启用邀请活动时，所有参与活动的个人套餐必须具有明确生效规则。

_Avoid_: 会员赠送积分、模型积分价格

## 模型展示配置（Model Presentation）

由后台维护并通过系统模型目录下发的模型显示信息，包括显示名称、模型图标、推广文案和促销角标。用户端的模型名称、选项标题和悬停标题只使用模型显示名称，不拼接供应商或渠道名称；编码值仍保留渠道身份用于请求路由。展示配置不改变供应商协议或计费口径。

_Avoid_: 前端模型皮肤、渠道图标配置

## 模型推广文案（Model Marketing Copy）

运营方为模型配置的简短能力说明。当前选中的模型默认在名称下方显示该文案；其他模型仅在用户悬停对应选项时显示，移开后收起。留空时该模型选择项始终保持单行。该文案不构成价格、性能或服务等级承诺。

_Avoid_: 模型描述、供应商说明

## 模型促销角标（Model Promotion Badge）

运营方附加在模型名称旁的短促销标识。角标是展示信息，不代表系统已自动修改积分价格或活动有效期。

_Avoid_: 模型折扣、计费折扣

## 模型访问策略（Model Access Policy）

系统模型的服务端授权规则。`authenticated` 允许所有已登录用户调用；`member` 只允许拥有有效个人订阅或有效团队席位的用户调用。前端锁定状态只是策略的展示，实际生成入口必须再次校验。

_Avoid_: 前端会员开关、模型可见性

## 有效会员（Active Member）

当前时间处于有效订阅周期内的个人订阅用户，或处于有效团队订阅下且团队席位状态有效的成员。Origin 基础权益不构成有效会员身份。

_Avoid_: 登录用户、积分用户、Origin 用户

## 团队协作画布（Team Collaborative Canvas）

归属于一个团队、由团队成员按权限共同访问的画布。画布创建者仍是内容归属与移回个人空间的责任主体；团队所有者和管理员拥有画布管理权限。团队订阅失效后画布保持可读，但停止接受内容修改和生成任务。

_Avoid_: 共享画布、公共画布

## 画布协作权限（Canvas Collaboration Access）

团队成员对单个团队协作画布的有效权限。权限分为查看、编辑和管理：查看只能读取；编辑可以修改内容并创建生成任务；管理还可以配置团队默认权限、成员覆盖权限、公开分享和删除。团队角色与单画布覆盖共同决定最终权限，服务端是唯一授权真源。

_Avoid_: 前端只读开关、成员角色

## 画布版本（Canvas Revision）

团队协作画布在服务端维护的单调递增版本号。每次成功持久化的画布变更只增加一次版本；提交方必须声明基准版本，基准不一致时必须显式重新同步，不能覆盖其他成员已提交的结果。

_Avoid_: 更新时间、前端版本

## 画布变更（Canvas Change）

对团队协作画布中节点、连线或画布级字段的一次已授权增量修改。画布变更具有客户端幂等标识、提交人、基准版本和提交后版本，并作为审计与断线恢复的事实记录。

_Avoid_: 自动保存快照、临时补丁

## 在线状态（Canvas Presence）

协作成员当前连接、光标和选择状态的短生命周期信息。在线状态只用于实时交互提示，不构成持久化画布内容；断线后由连接生命周期清除。

_Avoid_: 成员活动日志、画布变更

## 团队权益（Team Entitlement）

有效团队订阅快照中可执行的商业能力与额度，包括共享资产、任务队列策略、团队存储、项目权限、开票和商业使用授权。套餐展示必须来自同一权益真源，不能用独立营销文案代替实际授权。

_Avoid_: 团队套餐文案、前端权益开关

## 团队积分池（Team Credit Pool）

由团队订阅充值、供有效团队成员调用模型时共同使用的积分账户。每笔预扣、消费和退款都归属于团队，同时记录实际调用成员；成员月度额度限制作用于该成员从团队积分池产生的预扣与消费。

_Avoid_: 团队所有者个人积分、成员共享余额

## 会员商城展示配置（Membership Storefront Presentation）

由后台会员管理维护、用于个人会员与团队会员购买页的运营展示事实，包括活动倒计时、页签与区块文案、活动项、公共与独家权益、套餐层级生成摘要、生成数量矩阵、规则说明和常见问题。它只负责展示，不得重复定义套餐价格、周期积分、并发、席位或团队授权；这些可执行商业事实仍以会员套餐为唯一真源。

_Avoid_: 套餐价格配置、会员权益快照、前端写死文案

## 团队资产（Team Asset）

归属于团队范围、仅对有效团队成员开放的持久化媒体资源。创建者只是审计主体，不拥有将团队资产带回个人空间的隐含权利；成员离开后团队资产仍归团队。

_Avoid_: 成员个人素材链接、公开素材

## 项目访问权限（Project Access）

团队成员对单个团队项目的有效查看、编辑或管理权限。显式项目成员覆盖优先于团队角色映射；服务端在每个读写入口判定，前端状态不构成授权。

_Avoid_: 项目成员标签、页面只读状态

## 开票申请（Invoice Request）

用户针对已支付且具备开票权益的会员订单提交的开票业务记录。申请、审核、开具或驳回均为可审计状态；只有后台或已配置税务服务商回填发票号码和文件地址后才算已开具。

_Avoid_: 自动开票承诺、支付凭证
