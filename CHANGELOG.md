# HMaigc 更新日志

本文件从 HMaigc 商业版本开始记录，不继承上游项目的历史发布说明。

## 未发布

## v1.0.55 - 2026-08-24

### Agent 升级预检

- 升级切换前由目标版本镜像只读审计全部 `queued`、`running`、`waiting` Agent Run，一次返回候选数量、可安全退休数量和完整阻断原因；审计失败或存在阻断时不会替换当前业务服务。
- 安全退休仅收敛尚未跨越供应商、账务和 Artifact 商业边界的遗留运行；已启动工具、活动供应商请求、未决账务或不安全 Artifact 会显式阻止升级并保留原始事实。
- 部署编排在在线预检、停机后二次预检、目标版本启动和健康检查各阶段保持可追溯失败语义；任一门禁失败都会保留或恢复原版本，避免半升级状态。

## v1.0.54 - 2026-08-24

### Agent 升级迁移

- 修正旧暂停 Agent Run 的升级退休判定：同一 Run 中已经成功或失败的历史工具调用即使保留真实 `startedAt`，也不会被误判为仍在执行；迁移只关闭当前未启动的等待工具和等待资产，并完整保留历史工具状态、输出与审计时间。
- 当前 `running` 工具、未结束但已有启动事实的工具、已启动 pending checkpoint、活动供应商任务、未决账务和不安全 Artifact 仍会显式阻止升级；批次事务、幂等与自动回滚契约不变。

## v1.0.53 - 2026-08-24

### Agent 任务运维

- 管理后台新增跨用户 Agent 运行目录，按服务端权威运行状态、活动分类、用户和项目/画布作用域筛选未结束任务，并展示关联模型任务、媒体任务、账务与供应商请求事实；普通用户不能访问，列表不暴露提示词、正文、工具参数或凭据。
- 管理员可在详情确认真实影响后填写审计原因和一次性确认短语，以 `stateVersion` CAS 终止卡住的运行。终止会原子收敛 Run、Checkpoint、时间线、工具和 Artifact 状态；未提交供应商的任务取消并退回预留积分，已提交任务请求取消并进入账务核对，界面不会误报供应商计算已经完全停止。
- 状态冲突返回最新 Run 并要求重新确认；未决账务、任务与账单归属不一致或事实损坏会显式阻断终止。升级迁移仅安全退休尚未跨越商业边界的旧契约运行，活动账务或供应商事实不完整时保持原数据并阻止升级。

### 首页加载性能

- 首页与登录页先稳定展示轻量海报，窗口加载完成后经过明确的首屏稳定期并再次进入浏览器空闲状态，才加载背景视频；避免大体积媒体参与首屏 LCP 竞争，同时保留低动态偏好和真实加载失败提示。

## v1.0.52 - 2026-08-24

### 国内静态交付

- CDN 发布门禁按阿里云真实压缩资格校验入口资源：1 KiB 至 10 MiB 的 JavaScript/CSS 仍强制要求 Brotli/gzip，小于 1 KiB 或大于 10 MiB 的资源允许 identity 交付；HTTP/2/3、内容类型、不可变缓存、精确 CORS 与禁止重定向等安全和性能契约保持不变。

## v1.0.51 - 2026-08-24

### Agent 对话体验

- Agent 运行建立后、首个用户可见回复增量到达前，以星形图标、流光“思考中”和默认展开的真实事件轨迹展示执行状态；首个增量到达后自动收起为可再次展开的“已思考”，同一无标题助手正文在下方持续增长。轨迹不展示模型原始推理正文，也不使用前端伪流式、伪步骤或计时进度。
- Agent 输入框随真实内容自动增高，运行中继续使用原位停止操作；脚本节点和 Agent Composer 统一使用画布圆形提交按钮，不改变已确认的对话布局层级。
- 为版本字段引入前已终结且最终 Checkpoint 完整一致的 Agent Run 补记首代 Runtime/Policy 契约版本，并以只读投影补齐旧状态缺失的集合字段；仅在执行模式本身缺失时标记 `historical`，已有模型、Skill 与执行模式事实保持不变。迁移不改写事件、Checkpoint、内部模型任务或账单事实，异常零值记录会显式阻止启动。

### 模型能力与计价

- Agent Token 计价将平均输出量与最大输出上限拆分：平均值继续用于成本估算，最大值独立用于请求硬限制和最坏成本预留；一次性数据迁移保留历史估算值并补齐上限字段，不改写既有账单。
- 筷子 Kling 3 Omni 增加普通有声独立价格档位；后台能力目录只对 `std/pro` 发布同步音频，只对 `std/pro` 发布参考视频，`4k` 仅允许普通无声生成。画布、Agent 和供应商入口共同拒绝未发布组合，避免失败请求和错误报价。

### 首页创作体验

- 首页与工作区统一使用同一站点顶栏；首屏技能快捷项直接写入 Agent 草稿并显示选中态，发送后由运行冻结事实展示本轮模型与 Skills。
- 首页技能卡高亮信息色收敛为全局设计 Token，并保留轻量可读性遮罩；路由切换和低动态偏好继续使用真实加载状态，不制造伪进度。

### 画布媒体默认参数

- 新建图片与视频节点统一默认使用 16:9；视频节点默认使用 720P。节点已保存参数与用户后续手动选择继续优先，不改写既有作品。

### 国内静态交付

- 正式 Web 静态资源统一由 `static.hm.kunagent.com` 中国大陆 CDN 读取，香港 OSS 仅保留为发布写入与回源端点；生产 CSP、Nginx 门禁与 Chromium 收银台验收共同拒绝静态域名漂移。
- 收银台 Chromium 夹具同步公开法律文档独立接口，持续覆盖会员协议已发布与未发布状态，避免启动性能优化后发布门禁失真。

## v1.0.50 - 2026-08-21

### 画布媒体播放稳定性

- 将画布图片与视频的已鉴权播放签名和用户主动复制的短期 OSS 链接拆分：播放会话使用 4 小时签名，复制链接仍为 5 分钟，避免长时间编辑或后续 Range 请求因签名过期进入“媒体加载失败”。
- 播放地址继续按当前用户校验资源归属并禁止缓存，不公开永久 OSS 地址，也不改变资源存储、生成和计费链路。

### Agent 一致性生产链

- 为生产计划新增角色、道具、服装与场景参考资产契约；参考图先独立生成并形成可追溯 Artifact，分镜生成必须按声明顺序使用已成功的真实参考资源。
- 画布提交会落地参考图节点、逐镜头参考绑定与连接关系，避免把参考资产伪装成零时长镜头，并将 Agent 工具 schema 升级至 v3。
- 个人画布的 Agent 与媒体生成统一从个人积分账户计费，不再因用户同时加入团队而错误占用团队积分。
- 套餐权益与活动任务并发按账单冻结的个人或团队账户分别核算；Agent 幂等重放会校验同一计费账户，避免跨账户复用任务、额度或账单事实。
- 尚未同步到服务端的本地画布会明确阻止云端生成和报价，不再把未知画布标识隐式归入个人计费账户。

### 平台第一方技能库

- 将技能目录、激活、收藏和 Agent 运行时加载从外部社区接口硬切到 HMaigc 自有数据库，移除请求链上的远程列表、逐项状态查询和图片代理依赖。
- 新增不可变技能版本、SHA-256 校验和启动一致性门禁；Agent run 冻结精确技能版本与校验值，历史运行不会被后续技能更新改写。
- 首批发布短剧总导演、商业广告导演、故事开发、剧本撰写、悬疑视觉导演和分镜连续性导演六项平台技能；技能页只展示真实版本、来源与授权事实，不再伪造热度、评分或使用次数。
- 发布短剧总导演与分镜连续性导演 V3：补齐戏剧节拍、潜台词、表演调度、镜头职责、声音剪辑、镜头卡、首尾帧承接、对白动作账本和逐镜头证据自检；升级只追加新版本，不改写历史版本事实。

### 创作端一致性与性能

- 首页、项目页与技能页统一商业创作工作区的布局、色彩、账户菜单和团队入口；个人与团队项目继续使用明确的权限和计费作用域，不以界面状态替代后端事实。
- 首页首屏、账户增强能力和画布媒体运行时改为按需加载；新增画布拖动性能基准和 bundle 预算门禁，避免高频拖动链路重新引入全量派生与阻塞工作。
- 图片、视频、音频节点与 Agent 对话框共用同一套动态模型目录、模型行、菜单尺寸和选择状态；不再维护页面级模型选择样式分支，也不回退到硬编码候选。

## v1.0.49 - 2026-08-19

### Agent 结构化追问

- Agent Runtime 新增 `waiting_input`、结构化单选/多选/自由文本问题、逐题保存和不可变问答历史；回答继续同一条 Run，不创建第二条执行链，也不额外产生模型 Token、工具、媒体任务或计费事实。
- 回答接口使用 `stateVersion` 并发控制、稳定追问身份和严格 DTO；完全相同的重放保持幂等，冲突、过期版本、非法答案及非等待状态均返回明确错误与最新状态版本。
- 画布 Agent 面板新增“询问中”、紧凑问题卡、只读历史和失败草稿保留；普通输入在等待回答期间禁用，但本轮模型、Skills、附件与执行模式配置保持不变。

### 站点品牌与账户界面

- 首页主口号改由后台站点设置统一维护，默认值更新为“让算力更有想象力！”，公开站点配置、后台表单与首页展示使用同一字段契约。
- 首页账户菜单统一字体、字号、行高、图标和交互状态；法律富文本继续只允许受控文本对齐，同时接受浏览器序列化产生的合法尾随分号。

### 邮箱验证码

- 注册验证码邮件升级为品牌化 `multipart/alternative` 模板，同时提供纯文本和兼容主流邮件客户端的表格布局 HTML；动态站点名称、口号与版权信息均安全转义。
- 邮件明确展示真实 10 分钟有效期、分组验证码和安全提醒；邮件头拒绝换行注入，发送失败继续删除未送达的验证码事实。

## v1.0.48 - 2026-08-18

### Agent 升级安全

- 修复工具 schema v1 遗留 Agent 任务导致新版后端拒绝启动的问题；仅允许在 v1 初始运行事实完整且尚未开始工具执行时进行版本化退役，并在同一事务追加明确的失败事件与终态检查点。
- 升级前同步核对并锁定关联模型 Task 与 BillingOrder：未进入商业边界或任务与账单已经完整终结时才允许迁移，运行中、预留中、账实冲突或事实损坏时保持原数据并显式阻止升级，避免任务丢失、重复扣费或审计失真。
- 发布门禁新增真实 PostgreSQL 旧任务升级回归，并修复全量集成测试的必需用例发现逻辑；后端、Web、PostgreSQL/Redis 和部署升级/回滚演练均纳入本版本验收。

## v1.0.47 - 2026-08-18

### 首页加载性能

- 优化首页首屏图片与视频的加载优先级、预加载提示和视频首帧策略，将技能区移出首屏关键路径；Lighthouse 失败证据会在持续集成中完整保留，避免性能回退被静默忽略。

### 画布情绪预览

- 情绪工作区改为直接预览当前选中人物的真实图片与情绪定位，不再创建独立 WebGL 假人渲染器，避免 GPU 上下文竞争导致空白预览。
- OSS 与供应商图片会先解析为稳定的同源数据再做人脸检测和最终合成；完整原图事实随生成任务传递，避免临时地址失效或局部裁剪污染最终结果。
- MediaPipe Worker 改用同源模块加载路径，消除内容安全策略对 Blob 模块导入的拦截，并新增情绪面板与 OSS 解析回归测试。

### 会员权益准确性

- 移除当前系统尚未提供的“720 度全景”默认会员权益文案，避免将未实现能力作为商业权益对外展示。

## v1.0.46 - 2026-08-18

### Agent 生产运行时

- 将剧本、分镜图、视频片段和画布提交硬切到单一可恢复 Agent 生产运行时；生产计划和 Artifact Ledger 按租户、项目、画布及版本严格隔离，刷新、重试和跨进程接管不会重复执行已冻结动作。
- 收口 Agent 媒体任务的计费、取消、资产交接和最终交付核验；后台上游账单核对失败会写入结构化日志，不再静默丢失故障事实。

### 团队商业协作

- 团队创建、邀请、成员策略、共享资产和项目权限统一使用事务、幂等键、并发版本与追加审计；过期邀请持久化、一次性邀请凭证、席位竞争和跨团队权限均有明确契约。
- 团队共享资产上传在出站前分配稳定资源事实，成功或失败与审计同事务保存；团队项目创建者继续受有效订阅和角色权限约束。
- 团队前端写操作与刷新状态分离，请求使用代次隔离，避免迟到响应覆盖新团队数据或刷新故障吞掉一次性邀请凭证。

### 画布媒体预览

- 图片与视频预览统一读取账号资源的稳定播放地址，显式展示加载失败并提供原地址重试，不再依赖供应商临时链接或静默回退。

## v1.0.45 - 2026-08-16

### 单一 Agent Runtime

- 建立服务端单一主 Agent 的可恢复模型决策循环、通用交付验收、工具调用冻结、审批和只读画布事实读取；模型步骤与工具执行状态使用独立版本，不因刷新或重复提交重复消费。
- 接通 `canvas.apply_ops` 确定性修改：个人画布所有者与团队 editor/manager 共用现有 `CanvasChange` revision CAS 和幂等事务，审批后冻结完整 patch，提交中断可跨进程恢复，revision 冲突和非法参数作为结构化工具失败回灌主 Agent。
- Agent 模型调用继续复用既有 Task、BillingOrder、Provider 冻结和 Worker lease 商业链路；`generation.submit` 与 `generation.wait` 已接通真实生成任务、计费幂等、终态资产和通用交付证据，Web 面板与旧 Agent 硬切仍按后续里程碑实施。
- 新增作用域严格的 Agent REST/SSE 入口，事件只按数据库 sequence 增量传输并支持 `afterSequence` 断线续读；跨用户、跨画布和过期权限均重新核验，响应禁止缓存。
- 主 Agent 每个模型步骤冻结当前用户可调用的媒体模型、价格与能力事实，不传递供应商地址或密钥；生成等待由 worker 终态通知和后台持久化核对共同恢复，浏览器刷新不再承担唯一续跑责任。

### 计费与任务一致性

- Agent 自动提交图片或视频任务前使用后端唯一计价内核取得当前单任务报价版本与指纹，再进入正式 Task、BillingOrder 和积分预留事务；价格变化仍显式失败，不绕过媒体报价保护。
- 筷子 DeepSeek 文本模型的 Token 用量计费能力由后端模型目录统一发布，后台只允许受支持模型选择该计费方式；上游账单仍是最终结算权威，本地 Token 核算差异、缺失或无效用量会作为可审计警告保留。
- 修正 Token 实际扣费超过预留上限时的核对状态与租约释放，避免自动核对永久循环；个人和团队积分流水保存每一步真实余额快照，重复结算继续按完整事实幂等校验。
- 画布批量生成取消改为逐任务独立收敛；取消失败保留运行所有权和可重试任务事实，任务创建响应晚于用户停止操作时会在取得真实 task ID 后立即请求后端取消，避免已创建任务失去追踪。

## v1.0.44 - 2026-08-14

### 生产启动修复

- 修复 PostgreSQL 将部分唯一索引条件规范化为带类型转换和多层括号的等价表达式后，后端完整性门禁误判索引定义错误并拒绝启动的问题；表、列、唯一性和条件语义仍保持严格校验。
- 新增 PostgreSQL 规范化表达式回归测试，并通过真实 PostgreSQL、后端全量测试、静态检查与构建验证。

## v1.0.43 - 2026-08-14

### 统一资源存储

- 停止向用户提供个人 OSS 配置，删除个人设置中的“我的 OSS”入口与用户级读写接口；所有新上传和新生成资源统一使用后台平台 OSS，避免用户密钥、跨域与故障排查形成双轨。
- 历史资源继续按创建时冻结的个人 OSS 配置版本读取，现有资源不会因硬切换失联；数据库历史记录暂不删除。

### 账号级 AI 水印

- AI 水印配置硬切为账号级同步：用户在统一弹窗阅读管理员发布的管理规则与外链水印规范后，可开启或关闭去水印；设置跨设备生效，新规范发布后原同意自动失效并要求重新确认。
- 管理后台新增不可变版本发布，管理规则使用安全富文本编辑，水印规范仅接受 HTTPS 外链；发布、用户同意、偏好写入和失败结果均保留可追溯事实。
- 图片与视频节点删除水印开关及本地缓存双轨。任务创建和重试在同一事务中冻结账号偏好、协议版本、模型能力、供应商参数与结构化任务日志；受控模型显式发送水印布尔值，不支持的模型完全省略参数。
- 水印规则读取改用共享锁，保证发布与任务快照一致的同时避免并发任务互相阻塞；独立 PostgreSQL 门禁覆盖并发发布、偏好回滚、任务/计费/凭据/水印日志原子冻结及并发读取。

### 管理后台

- 管理后台硬切为统一的 Pro 数据页体系，概览、用户、邀请、会员、模型渠道、音色、超分、分镜提示词、积分、积分商城、兑换码、公告、法律协议与系统设置共享同一页面层级、表格、筛选、表单和弹层契约。
- 新增后台独立的明暗主题、菜单主题、品牌色、侧栏宽度、固定/流式内容宽度与顶栏滚动设置；配置仅影响后台，不污染创作端主题，并在桌面、平板和移动端保持一致导航行为。
- 修复浅色主题文字、占位符、状态标签、按钮、固定表格列和套餐编辑弹层对比度不足的问题，统一后台中文字体栈、字号、字重和行高层级。
- 模型管理入口收敛为单一模型中心，统一渠道、筷子账号、价格和 Agent 默认模型配置，删除平行旧入口与重复页面。

### 画布与生成节点

- 图片节点按后台发布的真实模型能力动态展示比例、分辨率、画质和供应商参数；模型未发布能力时显式失败，不再回退到前端硬编码候选。
- 图片、视频、音频及任务节点统一表面、边界、空态、选中态、参数弹层和提示词面板视觉契约，并移除视频节点不可用的独立超分入口。
- 优化画布拖拽、框选和连接预览热路径，避免位置变化触发无关的全量派生与高频 React 更新。

### 商城与发布可靠性

- 会员商城与积分超市统一关闭行为，关闭后返回首页；付款弹窗打开时继续由付款流程独占关闭操作，并同步无障碍名称和生产 Chromium 回归契约。
- 发布门禁完整覆盖后端、PostgreSQL/Redis 支付集成、Web 全量测试与构建、Nginx 哨兵、126 个生产 Chromium 收银台场景、Compose、安装升级及回滚编排，任一步失败都会阻止静态资源、镜像和正式 Release 发布。

## v1.0.42 - 2026-08-13

### 本地运行可靠性

- 本地 Compose 移除相对数据目录默认值，并新增统一启动脚本：无论从主项目还是 worktree 启动，后端都只挂载 Git 主项目唯一的 `.local/data`；未显式绑定数据目录时直接拒绝启动，避免误建空数据库导致原账号不可登录。自动化测试继续使用临时目录或退出即销毁的独立容器与数据卷。

### 模型渠道

- 筷子科技新增 GPT 5.5 与 DeepSeek V4 Pro 两个 Agent 文本模型；全部筷子模型统一使用后台唯一的 write-only Key 和 Base URL。管理员在商业定价页选择唯一的全站 Agent 默认模型，画布不再向普通用户暴露模型切换入口；后端任务与计费入口同步强制该默认值，不接受绕过前端指定其他模型。GPT 5.5 开放图片理解与工具调用，DeepSeek V4 Pro 明确仅支持文本。
- 筷子科技新增 GPT Image 2（`kz_gpt_image2`）图片系列：支持文生图、参考图生图、13 种画面比例、1K/2K/4K 与低/标准/高画质；任务继续复用冻结系列凭据、请求日志、失败核对、并发控制和资源存储。上游仅支持单张输出，因此画布选择该模型时不展示批量张数；种子、反向提示词、蒙版与透明背景未开放且会显式拒绝。
- 筷子科技配置页新增模型发布/同步入口：后端 registry 中的模型可一次事务发布到唯一兼容渠道并绑定已验证健康的系列凭据；未配置积分价格的模型始终保持禁用，避免误开放和零价调用。
- 筷子科技 Seedance 2.0 Fast/Pro/Mini 与 Seedance 2.5 统一硬切到 Ark 兼容异步接口，并与图片、Agent 模型共享同一账号凭据和跨渠道并发上限；删除旧 `/lz` 原生协议、独立 2.5 客户端及重复执行链路。
- 模型能力按官方 Model ID 由后端目录统一发布：2.0 支持 4–15 秒与 9/3/3 参考素材，2.5 支持 4–30 秒、30/10/10 参考素材且仅开放 480P/720P；画布、提交校验与执行端不再各自维护参数表或静默截断素材。2.0 的联网搜索能力已登记，但在独立计费尚未配置前只显示未开放状态且拒绝工具参数，避免产生未计价成本。
- 筷子账号余额校验改为读取官方 `wallet_balance`（分）字段，继续以精确十进制字符串展示，避免前端大整数精度损失。
- Seedance 用户售价按“分辨率 × 普通生成/参考视频”完整档位配置并冻结到计费订单；历史不完整档位不会向用户公开，管理员补齐全部档位后才可开放。任一历史账单仍待核对时禁止重试任务，避免重复创建和重复产生上游费用。
- 筷子所有携带 `ApiKey` 的请求统一禁止重定向并在拨号阶段复验目标地址；视频参考素材只接受当前用户拥有的资源标识，并使用数据库中的真实时长执行模型边界校验。
- 异步生成在请求发出前冻结不可重复创建边界，上游返回的任务 ID 优先写入任务和账单，再写运营审计；即使日志服务短暂失败也不会再次产生供应商费用。明确未受理的请求可安全重试，响应不确定或已有上游任务的请求必须先完成费用核对。
- Agent 默认模型、普通任务入口和兼容代理统一校验当前启用账号、活动地址、健康凭据与有效密钥；凭据失效后公共目录和默认模型会立即停止开放，不再只依赖前端隐藏。

### 画布与媒体生产

- 分镜表修改提示词、时长等生成事实时会同步更新已关联的视频节点；后端生成和重试路径统一保留资源标识、尺寸与时长，避免成功视频被误标为“未同步”；画布视频设置移除不可用的独立超分入口。
- 任务失败时，任务终态与积分退款或待核对状态改为同一数据库事务提交；任一侧写入失败都会保留可重试运行态，避免出现任务已失败但积分长期冻结的半完成状态。
- 用户取消与上游成功并发时，已生成结果、生成记录和存储配额事实继续保存，任务仍如实保持取消，费用转入后台核对；不再丢弃已经产生供应商成本的资产。
- 用户上传的音视频时长改由后端 `ffprobe` 从真实文件读取，前端时长只用于本地预览；生产后端镜像内置 FFmpeg，避免伪造时长绕过参考素材计费和模型边界。
- 已取消但供应商仍在运行的任务进入可持久恢复的核对队列；服务重启后继续查询同一个上游任务，成功资产仍写回原任务并保留取消事实，失败则保持费用待核对，不创建第二个付费任务。

### 发布可靠性

- Web 测试统一使用进程级 Happy DOM 环境，删除多个测试文件重复替换全局浏览器对象的实现；筷子后台凭据交互现在在 Linux/Bun 全量测试顺序下稳定验证，不再因 React DOM 绑定到过期 Window 而误报失败。

## v1.0.41 - 2026-08-13

### 发布状态

- 此不可变标签在 Linux Web 全量测试中暴露 Happy DOM 全局环境互相覆盖的问题，质量门禁如实阻断，未上传静态资源、未发布版本容器镜像且未创建 GitHub Release；其全部功能与测试环境修复由 v1.0.42 完整取代。

## v1.0.40 - 2026-08-11

### 会员与支付

- 修复账号尚未创建任何团队时，会员概览把空团队列表返回为 `null`，导致“开通团队会员”在打开付款弹窗前抛错且页面无响应的问题；生产 Chromium 现从 0 团队账号完成新建团队、创建订单和生成二维码全链路。
- 修复会员订单与积分充值的活动付款码到期后仍长期保持可支付状态的问题；后台生命周期任务现复用统一的订单锁、收银台锁和支付事实校验，将过期付款交易转入待对账并隐藏失效二维码。
- 未知或已失效的收银台 capability 统一返回不泄露内部事实的 404；关联订单损坏等服务端故障仍显式返回 500，避免把数据错误伪装成普通链接失效。
- 前端生产依赖审计由 17 项漏洞收敛为 0，并将仅用于构建与开发的 `shadcn` 移出运行时依赖。
- 会员商城右上角新增始终可发现的关闭入口：站内有来路时返回上一页，直接访问时回到首页；付款弹窗打开后由弹窗独占关闭操作，避免页面级入口误触。
- 修复个人会员真实冻结快照使用非团队席位范围 `0/0` 时被前端误判为无效、导致订单创建后无法继续打开收银台的问题；团队套餐仍严格校验冻结席位上下限。
- 团队会员付款弹窗与直达收银台按 880px 参考尺寸重构为 560/320 双栏，使用真实同档位周期套餐卡、席位步进器、连续订单明细和 128px 二维码静区；冻结订单后保持同一结构并锁定套餐与席位，平板和手机分别收敛为 400/320 与单列布局。
- 修复本地通过 `http://127.0.0.1:3000` 访问时会员购买写请求被开发环境 CORS 拒绝的问题；本地 Compose 现在明确允许 `localhost` 与 `127.0.0.1` 两个回环来源，生产环境白名单不变。

### 发布可靠性

- 生产 Chromium 门禁仅在首个案例、首次尝试且所有资源请求都明确失败为 `ERR_NETWORK_CHANGED` 时重建页面一次；HTTP 错误、连接拒绝、业务断言失败和第二次网络失败仍立即阻断发布，避免 GitHub Runner 启动时网络重配置造成假失败。

## v1.0.39 - 2026-08-11

### 发布状态

- 此不可变标签在生产 Chromium 质量门禁中连续遇到 GitHub Runner `ERR_NETWORK_CHANGED`，未上传静态资源、未发布版本容器镜像且未创建 GitHub Release；其功能与门禁可靠性修复由 v1.0.40 完整取代。

## v1.0.38 - 2026-08-11

### 会员与支付

- 会员付款弹窗在订单创建、创建失败、付款码恢复和支付终态中统一使用同一套左侧商品与订单明细结构，右侧仅切换配置、错误或付款状态，避免创建失败时退回旧版宽布局。
- 创作会员与团队会员付款弹窗按参考尺寸收紧为 766px 双栏布局，桌面保持 425/341 的订单与付款分区，平板等比缩放，手机改为单列自然滚动。
- 付款二维码固定为 112px 黑白 SVG，并置于 128px 白色静区内；生成付款码后不再重复展示支付渠道卡片，付款时效、协议入口与订单事实保持清晰可读。
- 新增独立的 HMaigc 会员服务协议后台发布项和公开页面；会员二维码下方提供安全的新窗口协议入口，协议未发布时如实提示，但不阻断创建订单、生成二维码、付款或会员履约。
- 会员弹窗创建 token 后首次读取失败时继续展示已验证的冻结订单事实和原始服务错误；未知类型的直达收银台在加载时使用中性识别态，首次读取失败时切换为非忙碌的中性失败态，不提前显示会员、续费或积分充值归属。
- 生产 Chromium 回归扩展至 126 个真实场景，覆盖个人、团队、积分充值、会员 token 首次读取失败、未知 token 首次读取失败、慢加载充值、亮暗主题、1440/768/390 响应式尺寸、协议发布状态及支付成功与失败终态。

### 发布可靠性

- 修复 GitHub Actions 低性能冷启动时会员套餐首屏超过 15 秒即误判失败的问题；门禁现等待套餐就绪或页面明确失败事实。受控 18 秒首次响应已完成 RED→GREEN，完整生产 Chromium 仍执行 126 个场景且不跳过真实错误。
- 生产浏览器门禁在模块加载失败时记录 HTTP 响应、请求失败、脚本错误、URL、加载态与页面文本；云端 `main` 已完整通过质量校验、静态步骤及 Web、后端、运维控制器三套镜像构建。

## v1.0.37 - 2026-08-11

### 发布状态

- 此不可变标签在生产 Chromium 质量门禁阶段遇到动态模块加载失败，未上传静态资源、未发布容器镜像且未创建 GitHub Release；其功能与可靠性修复由 v1.0.38 完整取代。

## v1.0.36 - 2026-08-11

### 发布状态

- 此不可变标签在生产 Chromium 质量门禁阶段失败，未上传静态资源、未发布容器镜像且未创建 GitHub Release；其功能与修复由 v1.0.37 完整取代。

## v1.0.35 - 2026-08-10

### 支付配置

- 修复后台使用单行密码框接收 PEM 凭据、导致商户私钥和支付公钥换行被浏览器删除而无法保存的问题；微信与支付宝的 PEM 字段现统一保留完整多行内容，API v3 密钥继续使用单行安全输入。

## v1.0.34 - 2026-08-10

### 支付可靠性

- 微信支付 Native 通道硬切为微信支付公钥模式：后台绑定 `PUB_KEY_ID_...` 公钥 ID 与对应公钥，创建、查单、关单请求统一携带 `Wechatpay-Serial`，响应与回调在验签前校验同一序列号，并以不含订单、二维码、凭据或 bearer 的安全日志记录渠道 `Request-ID`。

## v1.0.33 - 2026-08-10

### 发布可靠性

- 修复正式 Web 镜像的 CSP 仍允许未启用的旧 CDN 域名、却阻止实际 OSS 静态资源 Origin，导致版本升级后主脚本被浏览器拦截并显示黑屏的问题；发布门禁现从仓库静态资源变量核对脚本、样式、字体和 Worker 的真实 Origin。

## v1.0.32 - 2026-08-10

### 工作区体验

- 统一首页与项目、任务、素材、技能、团队、积分、设置及画布列表的站点 Logo、会员入口和账户操作，消除多套页头造成的尺寸与图标漂移。

### 会员体验

- 参考成熟会员商城的信息层级，收紧促销、计费周期与套餐卡片排版；
  统一单一推荐方案、主题色和移动端触控尺寸，并新增明确的页面关闭入口。
- 创作会员与团队会员购买硬切为单一支付弹窗：个人套餐直接创建订单，团队套餐先确认团队与席位，随后在同一弹窗内展示冻结订单明细和支付渠道返回的真实二维码，不再跳转到独立确认页。
- 重构创作会员、团队会员与积分充值收银台，以冻结订单快照展示金额、席位、周期积分和优惠；桌面、平板、手机及明暗主题保持同一支付事实与可访问交互。
- 支付轮询失败时保留已加载订单与二维码，刷新可恢复同一活动付款码；生成付款码后锁定渠道，终态以服务端事实收敛。
- 收银台重试与状态刷新操作固定满足 44×44px 触控热区，避免不同系统字体度量导致操作按钮过窄。

### 支付可靠性

- 会员下单、活动收银台和可支付交易增加数据库级幂等与并发约束；同一订单只保留一笔可能到账的交易，验签回调先持久化到账事实再统一履约。
- 微信与支付宝创建、查询、关单和回调统一校验签名、金额、币种及商户单；迟到、重复或不确定付款进入可审计的待复核状态，后台通过渠道对账处理，不再提供人工标记已支付入口。
- 活动付款码随收银台到期时原子转为待对账并隐藏二维码，保留唯一付款槽与渠道查单入口；所有者重试不会恢复已过期链接，而是引导关闭旧订单后重新下单。尚未接入真实退款能力的 `refunded` 枚举只显示待核对状态，不再向用户声称款项已退回。
- 收银台 bearer 使用哈希查询和加密恢复，应用与两层 Nginx 强制 `private, no-store`、`no-referrer` 并隔离敏感日志；生产 Chromium 以 72 个场景持续验证个人、团队、历史订单、积分、二维码和失败终态。
- 支付后台明确提示渠道启用后的真实扣款影响，并要求先完成公网 HTTPS 回调和小额支付验收，避免把正式商户参数误当成无扣款配置。

## v1.0.21 - 2026-08-05

### 升级可靠性

- 修复 MiniMax H3 供应商定价种子 ID 超出 PostgreSQL `varchar(36)`，导致 v1.0.20 后端迁移失败并触发自动回滚的问题。
- 增加定价种子标识长度回归检查，并验证从 v1.0.19 数据结构升级后后端能够正常通过健康检查。
- 升级验活失败时自动写入后端或 Web 容器状态及最近日志，后续故障可直接从升级日志定位根因。

### Agent 输入体验

- 优化首页与画布 Agent 输入框的模型、技能和自动模式选择反馈，已选能力在输入区内清晰展示并支持键盘连续删除。
- 统一输入区快捷操作图标与发送按钮的尺寸、语义和交互层级。

## v1.0.20 - 2026-08-04

### 会员与支付

- 会员下单后直接进入统一收银台，待支付订单支持继续支付，并在收银台展示真实渠道、金额、有效期与支付结果。
- 修复支付完成后收银台轮询无法读取已支付状态的问题；已消费令牌仅允许查看结果，禁止再次创建支付交易。
- 统一会员套餐公开名称、购买确认、订单操作和响应式布局，并优化登录后营销弹窗与首页品牌文案。

### 定价与结算

- MiniMax H3 视频与 MiniMax Speech/音色克隆新增供应商成本规格，并在调用日志中记录核算输入、成本结果和明确的核算失败原因。
- 视频超分从模型价格中拆分为独立定价模块，按版本、输出分辨率、帧率和视频时长计算附加积分，并把规则与供应商成本快照冻结到计费订单。
- 清理旧的 `SR_*` 模型价格双轨数据，基础视频生成费与超分后处理费分别核算、统一预扣和审计。

### 模型与视频生成

- 模型目录新增可运营的预计生成耗时，并在模型选择列表中以分钟展示，不再向用户暴露计费单价。
- 视频节点按渠道能力动态限制分辨率、时长、生成模式、音频和超分参数；Seedance Fast、Pro、Mini 分辨率边界与服务端校验保持一致。
- 视频生成恢复 1、2、4 条并行任务，分别记录生成结果与失败状态，部分失败时保留已成功产出的全部视频。

## v1.0.19 - 2026-08-03

### 站点运营

- 新增登录后营销弹窗，支持后台启停、活动图片、标题、说明、行动按钮和展示频率配置。
- 营销活动按用户与内容版本记录展示状态，支持单次、每日和会话级策略；活动内容变更后自动进入新一轮展示。

### 工程治理与性能

- 建立仓库内持久项目记忆索引，固定商业级交付约束、事实真源、开工检查、验收、提交与发布流程。
- 清理可再生的浏览器审计、Lighthouse、构建和发布缓存，同时保留正式部署脚本与受版本控制的视觉验收证据。
- 推进路由与画布重型模块按需加载，新增 bundle 预算和 Lighthouse 质量门禁，降低首屏主包压力。

### 模型体验

- 统一模型品牌图标的主题色规则：日间模式显示黑色，夜间模式显示白色，并覆盖画布与后台模型入口。

## v1.0.18 - 2026-08-02

### 管理后台

- 完成全部后台页面的视觉与交互统一，规范内容宽度、留白、字体层级、按钮、筛选栏、表格、表单、抽屉和响应式导航。
- 修复模型管理、音色管理、积分运营、法律协议、运行策略等页面在桌面、平板和手机尺寸下的内容裁切、控件挤压与横向溢出。
- 将用户协议与隐私政策编辑器升级为受限富文本编辑体验，并完善未保存状态、链接、图片与公开页面展示。
- 抽离后台表单请求与领域转换逻辑，补充登录设置、邮件、存储、兑换码、公告和用户管理的回归测试。

### 画布与媒体生产

- 统一画布工具栏、上下文菜单、节点悬浮操作、媒体节点输入区、提示弹窗和 Agent 控件的视觉与交互标准。
- 新增视频合成节点与编辑器，支持按画布连线顺序组合视频、调整素材片段、拆分时间范围并执行合成。
- 完善视频生成模式契约，支持文生视频、全能参考、图生视频、首尾帧和图片参考，并对必需素材执行显式校验。
- 优化音频节点、音色选择、语气词、停顿和声音克隆交互，统一图片、视频与音频节点的排版和模型选择样式。
- 加强 Agent 生成结果等待与交付校验，只有目标媒体节点产生真实资产后才确认完成，终态失败会明确返回原因。

### 质量与设计规范

- 更新全局设计真源与 Web 执行规范，固化商业后台、画布弹层、响应式断点和无障碍交互标准。
- 新增视频生成、视频合成、画布滚动条、法律富文本和后台表单领域逻辑测试。

## v1.0.17 - 2026-08-01

### 模型渠道

- 扩展系统渠道接口类型字段长度，修复 PostgreSQL 无法保存“AI 开放平台视频（火山兼容）”渠道的问题。
- 新增协议标识与数据库字段容量回归测试，防止后续增加接口类型时再次超过持久化约束。

## v1.0.16 - 2026-08-01

### 首页运营

- 新增后台可维护的首页运营横幅，支持启停、标签、正文、投递与分享按钮文案及链接配置，修改结果直接作用于首页展示。
- 运营横幅在移动端自动隐藏，避免窄屏内容拥挤和信息截断。
- 统一首页与工作台的账户算力视觉，将闪电图标调整为一致的蓝色语义强调色。

## v1.0.15 - 2026-08-01

### 管理后台

- 重新梳理管理后台的信息架构、页面标题、工具栏、表格、表单、抽屉、会员管理和数据概览，统一字体、间距、控件高度与响应式规则。
- 重构桌面侧栏与移动端导航抽屉，优化分组、搜索、选中态、折叠状态和触控尺寸，消除窄屏重叠与横向溢出。
- 将用户协议和隐私政策从站点品牌配置中独立为“法律与协议”，新增受限富文本编辑、后端 HTML 白名单校验及独立管理员审计记录。

### 认证合规

- 登录、注册及 Linux.do 授权入口统一增加用户协议与隐私政策显式勾选；未同意时前端阻止提交，不再以静态说明代替真实授权。
- 公开法律页面支持安全渲染后台维护的结构化内容，同时保持脚本、事件属性和非白名单标签不可进入存储。

## v1.0.14 - 2026-08-01

### 发布可靠性

- 修复版本化静态资源与 Web 镜像分别构建导致入口文件内容哈希不一致、升级后页面黑屏的问题；标签发布现在只构建一次前端，并让 OSS/CDN 与 Web 镜像共同使用同一份已验证产物。
- 发布门禁会核对入口 JS/CSS 是否属于当前不可变版本目录、本地构建产物是否真实包含对应文件，以及 OSS/CDN 是否可以实际访问，任一条件失败都会阻止镜像发布。
- 部署验活新增 SPA 入口资源检查；即使健康接口与 HTML 正常，只要入口 JS/CSS 缺失，升级也会明确失败并执行自动回滚。

## v1.0.13 - 2026-07-31

### 认证体验

- 保留原有深色电影化双栏认证页面，仅统一优化登录与注册表单的输入框、按钮、提示状态和键盘焦点，并完整适配桌面、平板与移动端。
- 登录与注册保留真实站点名称、Logo、注册策略、邮箱验证码、邀请码和 Linux.do 配置；配置读取失败、注册关闭与邮件未配置均在表单内明确展示，不再静默隐藏。
- 精简注册表单，仅保留用户名、邮箱、验证码与密码等必要信息；显示名称由用户名初始化，不再要求用户重复确认密码。

### 存储与分发

- 新增管理员历史资源迁移任务，将后端数据卷中的本地媒体按不可变快照复制到平台 OSS；逐文件校验大小与 ETag 后才切换数据库引用，失败项可审计重试，原始本地文件不会自动删除。
- 新增迁移任务与迁移明细事实表，支持进程中断恢复、资源并发变更拦截、完成统计和失败原因追踪。
- 新增后台“存储服务 → 历史资源迁移”操作区，展示待迁移容量、执行进度、失败资源与显式确认入口。
- 新增版本化静态资源发布器；发布标签时先把 Vite 构建产物上传至独立 OSS/CDN 版本目录并逐个校验，再构建引用该版本目录的 Web 镜像。
- 静态资源使用一年不可变缓存并保留历史版本目录，应用回滚时继续引用对应旧版本资源；发布清单经 CDN 验证失败时阻止镜像发布。
- 修复用户删除素材后云端对象未清理的问题：仅删除不再被画布、其他素材、角色版本或音色引用的资源，OSS 删除失败时保留素材与资源记录并明确报错，迁移中的资源禁止并发删除。

## v1.0.12 - 2026-07-31

### 运维与发布

- 新增管理员专用运维升级中心，集中展示远程版本检查、备份完整性、升级、回滚、实时日志和独立审计记录。
- 新增与业务栈分离的部署控制器和 CLI；业务后端通过 Unix socket、HMAC 签名、时间窗与 nonce 防重放协议提交异步任务，不再持有 Docker socket 或自行重启。
- 控制器使用独立 SQLite 审计库和 `ops-state` 持久卷，即使业务数据库回滚或后端重启也不会丢失运维事实。
- 备份辅助容器统一通过命名卷访问恢复点，避免把控制器容器内部路径错误地当成宿主机 bind mount。
- 控制器统一复制并收紧生产配置权限，修复自定义配置路径和 Docker Desktop bind mount 权限不一致导致的首次安装失败。
- Web 验收改为从业务容器内部核对健康接口与 SPA 深链接，并修复脚本结束时日志管道关闭被误报为主任务失败的问题。

## v1.0.11 - 2026-07-31

### 生产发布

- 从 Web 源码、依赖、发布镜像和 CI 工作流中彻底移除第三方绘图编辑器及其许可证密钥要求。
- 删除画布绘图节点、专用本地存储、图片引用转换、导入导出和复制粘贴链路，统一保留可商业运营的文本、图片、视频、音频、分镜、技能、配置与背板节点。
- 本地画布、云端同步、多人协作和文件导入会清除历史绘图节点及其关联连线，避免退役内容继续出现在前端。
- 首次安装和后续升级不再要求配置绘图编辑器授权，避免发布流水线因缺少第三方许可证密钥失败。

## v1.0.10 - 2026-07-31

### 商业运营

- 建立个人会员与团队会员体系，支持套餐、席位、并发、共享资产、积分额度、项目权限和团队审计。
- 增加邀请有礼、兑换码、积分运营、模型商业定价、成本与利润率配置。
- 模型支持会员专属授权、自定义图标、运营文案和促销角标。
- 管理后台集中维护站点品牌、Logo、版权、用户协议、隐私政策及备案信息。
- 更新日志改由管理员专用接口按需读取，不再进入普通用户前端资源。
- 首页与工作台会员入口根据有效订阅状态显示“升级会员”或“会员中心”。

### AI 创作与媒体能力

- 用户端统一使用后台配置的系统模型，模型调用接入积分扣费与请求明细。
- Agent 收敛为网站服务单一路径，移除用户端 API、模型配置和内部日志入口。
- 打通首页对话到画布 Agent 的可恢复创作流程。
- 完成图片、视频、音频节点的模型选择、生成参数和统一交互。
- 接入 MiniMax 语音合成、系统音色试听、私有音色克隆与收藏。
- 增加 AI 视频水印偏好设置，视频节点可显式覆盖全局设置。
- 视频生成支持参考图、首尾帧、声音参数、分辨率定价和超分能力配置。
- 接入 AI 开放平台火山兼容视频协议。

### 协作与工作台

- 增加团队空间、成员邀请、席位权限、多人画布实时协作和共享资产库。
- 重构首页、项目工作台、素材库、任务、技能、会员中心和管理后台的统一视觉与响应式布局。
- 统一画布页头、快捷键面板、节点编辑器、模型弹层及连线断开交互。
- 修复中等宽度导航重叠、旧静态页面抢占路由和浏览器持续加载旧版本的问题。

### 生产部署

- 生产环境统一使用 PostgreSQL、Redis 和不可变 GHCR 版本镜像。
- 建立发布前 Go、Web、Compose 与部署编排质量门禁。
- 提供一键安装、备份、升级、验活和回滚命令。
- 升级失败时自动恢复 PostgreSQL、后端资源卷和上一版本镜像。
- 增加固定依赖镜像摘要、部署互斥锁、操作日志、备份校验和版本漂移拦截。
