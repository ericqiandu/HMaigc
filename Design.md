# HMaigc 全局设计规范

本文件是 HMaigc 商业产品的全局视觉真源。所有新增或重构的页面、组件、弹层、节点和交互必须先对齐本文件；`web/DESIGN.md` 只负责把这些规则映射到 Web 实现。

## 产品气质

- 专业、克制、清晰，面向长期使用的 AI 内容创作与商业运营。
- 信息密度优先于装饰；层级通过留白、对齐、字体和明度差表达。
- 首页、工作台、画布编辑器、会员购买页和管理后台可以拥有不同信息架构，但必须共享字体、语义颜色、控件尺寸和交互反馈。
- 不为复刻参考站伪造能力、数据、状态或操作。

## 字体

- 20px 以下优先使用 `SF Pro Text`，中文回退为 `PingFang SC`、`Microsoft YaHei`。
- 页面标题使用 `SF Pro Display`，中文回退保持一致。
- 页面标题：16px / 22px / 600。
- 卡片标题：14px / 20px / 600。
- 正文与控件：14px / 20px / 400–500。
- 辅助信息：12px / 17px / 400。
- 紧凑元数据：11px / 16px / 400。
- 同一语义层级禁止重复创造字号和字重。

## 间距与栅格

- 基础单位为 4px；常用间距只使用 4、8、12、16、20、24、32、48、64px。
- 工作台普通内容最大宽度为 1240px；普通页面框架最大宽度为 1448px。
- `collection` 与 `data` 页面在 1600px 以上保持 104px 页面边距；卡片不得无限拉宽。
- 桌面工作台控件高度为 36px；图标操作热区不得小于 32×32px。
- 移动端主要操作和输入控件不得小于 44px，页面边距为 16px。

## 颜色

- 页面和组件只使用所属界面的语义变量，不在业务组件中写死亮色、暗色两套颜色。
- 页面背景、表面、次级表面、控件背景、文本、弱文本、分隔线、主色、成功、警告和错误必须各自拥有稳定语义。
- 蓝色仅用于明确主操作、选中状态、进度和焦点。
- 日间与夜间模式保持同一信息层级，不通过降低文字对比度隐藏信息。

## 形状与层级

- 普通控件圆角为 8px；卡片和主要面板圆角为 12px。
- 只有搜索框、状态标签和语义明确的胶囊控件允许全圆角。
- 独立区块最多一层容器同时拥有可见边框和圆角；禁止圆角卡片套圆角卡片。
- 边框只用于真实信息边界；默认不使用渐变、纹理、重阴影和纯装饰描边。

## 页面与组件

- 每页只保留一个 `h1`。
- 页面标题、数量和页面级操作保持同一视觉节奏。
- 列表搜索、筛选和排序放在标题下方工具栏，不与页面级创建操作混杂。
- 同一工具栏按钮保持统一高度、字号、圆角和表面；次要操作在前，主要操作在后。
- 移动端两个及以上页面操作等宽换行；单个操作保持内容宽度，不使用隐藏横向滚动掩盖溢出。
- 仅图标操作必须提供 `aria-label` 和可见焦点。

## 工作台

- 项目、画布、素材、技能使用 `collection` 布局；任务记录使用 `data`；积分、团队和设置使用 `standard`。
- 项目卡片桌面标准为 236×189px；画布与素材卡片保持 240–260px；技能卡片保持 272–292px。
- 管理后台桌面侧栏为 236px；小于 1024px 时使用顶栏与抽屉，禁止把全部导航压缩为横向滚动条。

## 画布编辑器

- 画布工具条、节点编辑器和弹层使用一致的字体、图标尺寸、控件高度与反馈状态。
- 拖动、缩放和连线热路径不得因视觉效果引入全量数据扫描或同步持久化。
- 浮层不得遮挡节点主要操作；移动端优先保证可读缩放和 44px 触控热区。
- 生成失败必须展示真实原因，不使用伪成功、假进度或静默降级。

## 响应式与可访问性

- 390px：移动端单列，16px 页面边距，44px 触控控件。
- 768px：平板窄屏，工具栏允许分行，保留完整操作文案。
- 1024px：桌面布局起点，标题与操作优先同排。
- 1440px：完整桌面布局，正文有效宽度不超过 1240px。
- 1600px 以上：集合页可流式扩展，普通信息页继续保持 1240px 有效宽度。
- 所有交互必须支持键盘操作、可见焦点和 `prefers-reduced-motion`。

## 禁用项

- 禁止同层级混用多套字号、颜色、圆角和按钮高度。
- 禁止通过默认值、假数据或隐藏错误维持表面完整。
- 禁止增加未接后端的假按钮、假入口和不可执行状态。
- 禁止把历史前端、构建产物、源码压缩包或验收临时文件作为第二套运行时入口保留。
