# HMaigc 全局设计规范

本文件是 HMaigc 商业产品的全局视觉真源。所有新增或重构的页面、组件、弹层、节点和交互必须先对齐本文件；`web/DESIGN.md` 只负责把这些规则映射到 Web 实现。

## 产品气质

- 专业、克制、清晰，面向长期使用的 AI 内容创作与商业运营。
- 信息密度优先于装饰；层级通过留白、对齐、字体和明度差表达。
- 首页、工作台、画布编辑器、会员购买页和管理后台可以拥有不同信息架构，但必须共享字体、语义颜色、控件尺寸和交互反馈。
- 不为复刻参考站伪造能力、数据、状态或操作。

## 字体

- 全站使用单一系统优先字体栈：`-apple-system, BlinkMacSystemFont, PingFang SC, Inter, HarmonyOS Sans SC, Noto Sans SC, Microsoft YaHei, sans-serif`，并附带系统 Emoji 回退。该栈与参考产品实际加载方式一致，不下载网页字体。
- 页面标题与正文使用同一字体族，通过字号、字重和留白区分层级；禁止为标题额外引入展示字体或第二套中文字体。
- 页面标题：16px / 22px / 600。
- 卡片标题：14px / 20px / 600。
- 正文与控件：14px / 20px / 400–500。
- 辅助信息：12px / 17px / 400。
- 紧凑元数据：11px / 16px / 400。
- 同一语义层级禁止重复创造字号和字重。

## 间距与栅格

- 基础单位为 4px；通用间距只使用 Token：4、8、12、16、24、32、40、48、64、80px。业务样式不得创造 13、19、27、35 等孤立间距；旧代码迁移时按最接近的 Token 收敛。
- 工作台普通内容最大宽度为 1240px；普通页面框架最大宽度为 1448px。
- `collection` 与 `data` 页面在 1600px 以上保持 104px 页面边距；卡片不得无限拉宽。
- 桌面工作台控件高度为 36px；图标操作热区不得小于 32×32px。
- 移动端主要操作和输入控件不得小于 44px，页面边距为 16px。

## 颜色

- 页面和组件只使用 `web/src/styles/design-tokens.css` 中的语义变量，不在业务组件中写死亮色、暗色两套颜色。
- 深色主题基线为 `--bg-primary: #0B0F14`、`--bg-secondary: #111827`、`--bg-tertiary: #161B22`、`--bg-surface: #1B212B`；文本、边界、品牌、金色和遮罩全部从对应 Token 引用。
- 页面背景、表面、次级表面、控件背景、文本、弱文本、分隔线、主色、成功、警告和错误必须各自拥有稳定语义。
- 蓝色仅用于明确主操作、选中状态、进度和焦点。
- 日间与夜间模式保持同一信息层级，不通过降低文字对比度隐藏信息。

## 形状与层级

- 半径只使用 `8 / 12 / 16 / 20 / 24 / 999px` 六个 Token。紧凑控件、菜单项为 8px；普通按钮与卡片默认 12px；输入框、首页内容卡与重点表面可使用 20px；Modal 可使用 24px；头像与语义胶囊使用 999px。不得出现 13、17、21、22px 等孤立半径。
- 阴影仅使用 `--shadow-sm / md / lg / xl`，模糊仅使用 `--blur-sm / md / lg / xl`；业务组件不得自定义新的阴影、发光或模糊值。
- 动效仅使用 `150 / 220 / 300ms` 与 `cubic-bezier(.2,.8,.2,1)`；必须兼容 `prefers-reduced-motion`。
- 层级仅使用 Header 100、Dropdown 300、Popover 500、Modal 1000、Toast 1100，不得使用无语义的超大 z-index。
- 只有搜索框、状态标签和语义明确的胶囊控件允许全圆角。
- 独立区块最多一层容器同时拥有可见边框和圆角；禁止圆角卡片套圆角卡片。
- 边框只用于真实信息边界；默认不使用渐变、纹理、重阴影和纯装饰描边。

## 图标

- 通用操作、导航、状态反馈统一使用 Lucide 的线性图标；不得在同一操作语义中混用实心、彩色和不同线宽的图标集。
- 模型、渠道等品牌标识统一使用经过审核的 Iconfont 单色 SVG 资产，并由 `ModelBrandIcon` 以 `currentColor` 渲染；日间为深色、夜间为浅色，禁止以 `brightness`、`invert` 等滤镜临时修正主题。
- 业务组件不得直接内嵌新的品牌 SVG 或散落的图片 URL；新增品牌图标必须先进入 `web/public/icons/`，再登记到 `web/src/lib/model-brands.ts`。
- 通用图标默认 16px，紧凑元数据 14px，主要导航 18px；仅图标按钮的交互热区不小于 32×32px，并提供 `aria-label` 与可见焦点。

## 页面与组件

- 每页只保留一个 `h1`。
- 页面标题、数量和页面级操作保持同一视觉节奏。
- 列表搜索、筛选和排序放在标题下方工具栏，不与页面级创建操作混杂。
- 同一工具栏按钮保持统一高度、字号、圆角和表面；次要操作在前，主要操作在后。
- 移动端两个及以上页面操作等宽换行；单个操作保持内容宽度，不使用隐藏横向滚动掩盖溢出。
- 仅图标操作必须提供 `aria-label` 和可见焦点。

## 弹层与浮窗

- 所有新右键菜单、下拉菜单、Popover、Modal 和 Drawer 必须复用现有弹层语义与组件，不得在业务页面重新创造一套字体、颜色、圆角、阴影或按钮标准。
- 弹层共享视觉语言：紧凑信息密度、单层可见边界、克制阴影、明确对齐；禁止“外层圆角面板 + 内层圆角卡片 + 图标方块”的多层装饰堆叠。
- 紧凑菜单标准为 13px / 20px / 400，菜单项高 32px、圆角 8px；夜间表面 `#262626`、边界 `#363636`，日间表面 `#ffffff`、边界 `#e5e7eb`。
- 紧凑右键菜单使用 16px 外层圆角和 8px 内边距；阴影固定为 `0 8px 32px rgba(0,0,0,.15), 0 2px 8px rgba(0,0,0,.1)`，不得追加发光或重投影。
- 菜单宽度由内容类型决定：空白画布命令菜单为 196px；普通下拉、表单 Popover、Modal 和 Drawer 按内容契约确定宽度，禁止机械套用菜单尺寸。
- 弹层只保留有信息职责的标题、分组和分隔线；可由上下文明确理解的菜单不得重复展示标题或分组说明。
- 仅支持纵向滚动的弹层必须显式使用 `overflow-x: hidden` 与按需 `overflow-y: auto`，禁止出现横向滚动条，也禁止用隐藏滚动掩盖真实内容溢出。
- 日间与夜间模式必须保持相同尺寸、密度与层级；仅切换语义表面、边界和文字颜色。

## 工作台

- 项目、画布、素材、技能使用 `collection` 布局；任务记录使用 `data`；积分、团队和设置使用 `standard`。
- 项目卡片桌面标准为 236×189px；画布与素材卡片保持 240–260px；技能卡片保持 272–292px。
- 管理后台桌面侧栏为 236px；小于 1280px 时使用顶栏与抽屉，避免侧栏挤压运营表单和宽表格，禁止把全部导航压缩为横向滚动条。
- 管理后台的确认浮层必须复用统一语义入口，标题、说明、按钮间距和危险状态不得使用组件库默认外观；移动端浮层宽度不得超过视口减 24px。
- 表格主空状态与抽屉内紧凑空状态必须来自同一共享组件；紧凑变体只收缩垂直留白，不得改变图标、文字层级或错误语义。
- 管理后台页面、Grid/Flex 直接子级与表格外层必须允许收缩；宽表格只允许在自身内容区横向滚动，禁止以页面级 `overflow-x: hidden` 裁切内容来制造无溢出的假象。
- 小于 1280px 的后台顶栏、抽屉导航、筛选操作和普通表单控件触控热区不低于 44px；移动端不得依赖悬停或精确点击完成主要操作。
- 后台加载、空数据和失败必须是三种独立事实状态：加载使用统一骨架，空数据使用共享空状态，失败必须保留原因与明确重试入口，禁止用 Toast 后的空白界面掩盖请求失败。
- 后台自定义 Dropdown、Popover 和详情摘要必须复用后台语义表面、文字层级和触控标准，不允许页面内用零散 Tailwind 边框与阴影创建平行组件语言。
- 后台数据表格首屏加载使用共享表格骨架；保留旧数据的刷新只显示真实加载状态，不得把整张表格骨架塞入旋转指示器形成双层加载界面。
- 后台分页统一展示当前范围与总数，桌面页码热区不低于 32px、移动端不低于 40px；仅当用户确实可切换每页数量时展示页容量选择器。
- 后台设置表单只允许一层内容边距：共享设置卡已经提供内容边距时，内部字段网格不得再次叠加整块水平或顶部内边距。
- 设置表单必须明确区分“已同步”与“有未保存变更”；没有变更时保存按钮禁用，保存成功后恢复同步状态，禁止让用户反复提交相同配置。
- 复杂设置按职责分为带语义标题的字段区；低频高级字段使用可键盘展开的渐进披露，不将全部专业参数一次性堆叠给运营人员。
- 运维、回滚、备份等高风险操作必须在执行前展示影响清单与准确确认短语；不可用操作必须说明真实禁用原因，不得仅降低透明度。
- 同一时间只允许一个独立控制器任务时，页面必须持续展示活动任务、当前阶段、发起人和日志入口；所有冲突操作明确锁定，任务完成后再恢复。
- 高密度运营页不得把首次加载失败显示成零指标或空表；失败状态必须替代依赖该请求的指标和表格，并保留原地重试入口。
- 成本、售价等会直接影响商业结算的抽屉表单必须区分已同步与未保存状态；无变更禁止重复提交，保存期间禁止关闭。
- 音色目录必须支持按渠道、名称、标识、语言、类型与运营状态快速定位；渠道或目录读取失败必须保留原始原因和重试入口，禁止伪装为空目录。
- 供应商同步属于独立后台动作，不得阻塞或清空当前音色表；音色编辑与克隆抽屉必须保护未保存输入，保存期间禁止关闭。
- AI 模型配置必须分别保留渠道目录与渠道内模型目录的加载、已有数据刷新、失败和空数据事实；刷新失败时继续展示旧数据并提供原地重试，不得吞成空目录。
- 渠道和模型编辑器必须以真实脏状态控制保存与关闭；模型目录页头、筛选、结果计数和操作入口使用统一后台语义，不以零散边框与小按钮堆砌。
- 积分策略、人工调账和异常计费属于资金等价运营链路：策略读取失败不得显示可编辑空表单，无变更禁止重复保存，人工调账必须在写入前展示目标用户、积分变化和调整依据。
- 异常计费列表必须区分首次加载、保留旧数据刷新、失败与空数据；结算或退款弹窗必须展示冻结积分、模型与上游请求事实，写入期间禁止关闭。
- 带固定侧栏的后台复合表单不得仅依赖 Tailwind 视口断点决定多栏布局；必须按后台可用内容宽度设置断点，1280px 以下保持单列，所有 Grid 直接子级允许收缩。
- 存储配置与历史迁移是两个独立运维数据域：任一读取失败必须在对应区块原地展示原因和重试入口，不得让 OSS 配置失败落入可编辑空表单，也不得把迁移失败伪装成零资源。
- OSS 配置必须使用真实脏状态控制保存；密钥留空只表示保留已保存密钥。迁移与失败项重试必须展示目标、数量和准确确认短语，任务提交期间禁止关闭弹层或重复提交。
- OSS 配置刷新不得覆盖管理员尚未保存的输入；浏览器刷新或后台内部导航离开前必须确认，保存期间不触发重复离开保护。
- 启用 OSS 时，Endpoint、Bucket 与访问凭据必须在字段附近完成阻断校验；迁移概览读取失败时不得继续显示“尚未执行”等未经确认的状态。
- 兑换码批次与批次内明细必须分别保留首次加载、保留旧数据刷新、失败和空数据状态；创建成功后重新读取服务端批次事实，不得仅在前端拼接伪造分页结果。
- 批次禁用与单码禁用必须展示实际可用数量或兑换码尾号及不可恢复影响；写入期间锁定同类操作，生成结果需提供复制和下载两种真实交付入口。
- 支付配置加载失败时不得渲染可编辑空表单；微信与支付宝只展示各自协议真实需要的参数，禁止把微信证书、API v3 等专用字段堆入支付宝配置。
- 商户配置必须使用真实脏状态和字段级 URL 校验控制提交；保存期间锁定整张表单，密钥留空只表示保留已保存密钥，参数完整度与真实通道接通状态必须分别表达。
- 邮件服务配置首次读取失败时不得展示可编辑空表单；SMTP 端口必须以整数契约提交，启用状态下主机、发件邮箱和登录凭据必须在字段附近完成阻断校验。
- 邮件配置必须以规范化后的真实差异表达未保存状态；已保存密码留空只表示保持原值，保存期间锁定整张表单，接口未提供测试发信能力时不得伪造测试入口。
- 邮件配置存在未保存的 SMTP、发件人或密钥输入时，浏览器刷新和后台内部导航离开前都必须确认；字段校验失败必须停留在原表单并指向具体字段。
- 登录与注册配置必须把注册策略与第三方 OAuth 作为两个独立数据域加载和报错；任一接口失败不得把另一域清空，也不得展示可编辑的默认空配置。
- 第三方 OAuth 表单必须以规范化后的真实差异表达未保存状态；启用时 Client ID、密钥、HTTP(S) 地址、授权范围和账号唯一字段必须在字段附近阻断校验，保存期间锁定表单。
- 系统公告首次加载失败不得显示零记录或空表；保留旧数据的刷新失败必须明确标注数据来源并提供原地重试，不能只弹 Toast。
- 公告发布窗口必须说明即时生效范围、保护未发布输入并阻止重复提交；关闭公告必须展示目标标题与实际影响，历史记录不得被伪装为删除。
- 法律文档首次加载失败不得渲染可编辑空协议；保留旧内容的刷新失败必须明确说明当前显示的是上次成功结果。
- 用户协议与隐私政策必须按服务端同步基线判断真实改动；未保存内容离开页面前必须确认，编辑器工具栏在窄屏采用换行布局而非页面级横向滚动。
- 用户目录首次读取失败不得伪装为空用户列表；已有数据刷新失败时继续展示上次成功结果，并在表格区域保留原因和重试入口。
- 用户角色、启停状态属于访问权限事实；无真实改动禁止保存，变更前必须展示目标账号与实际差异，保存期间阻止关闭和重复提交。

## 画布编辑器

- 画布工具条、节点编辑器和弹层使用一致的字体、图标尺寸、控件高度与反馈状态。
- 桌面端 Agent 使用右侧停靠工作区，不覆盖画布；默认宽度 400px，可在 320–560px 间拖拽调整。面板开合或宽度变化时必须保持当前画布世界中心并以 O(1) 视口变换适配可用区域，禁止扫描全部节点或连接。
- 首页与画布 Agent 输入框共享提示词、参考图片、动态模型、公开 Skills 和执行模式的交互契约；选择项支持显式移除，空输入时 Backspace 只按可见顺序删除最后一个选择，不得引入隐藏默认模型。
- Agent 执行模式菜单保持两行紧凑事实文案与固定语义图标：手动模式使用手掌图标并显示“每次生成前询问”，自动模式使用芯片图标并显示“可逆操作自动执行，付费前确认”；禁止用循环箭头冒充自动模式，也禁止用“完全自动”等文案掩盖付费审批边界。
- Agent 与图片、视频、音频、文本及生成配置节点共用同一 32×32px、8px 圆角提交按钮；可用、执行中和停止态只改变语义图标与状态色，积分预估独立显示在按钮左侧。
- 拖动、缩放和连线热路径不得因视觉效果引入全量数据扫描或同步持久化。
- 浮层不得遮挡节点主要操作；移动端优先保证可读缩放和 44px 触控热区。
- 生成失败必须展示真实原因，不使用伪成功、假进度或静默降级。

## 响应式与可访问性

- 390px：移动端单列，16px 页面边距，44px 触控控件。
- 768px：平板窄屏，工具栏允许分行，保留完整操作文案。
- 1024px：桌面布局起点，标题与操作优先同排。
- 1440px：完整桌面布局，正文有效宽度不超过 1240px。
- 1600px 以上：集合页可流式扩展，普通信息页继续保持 1240px 有效宽度。
- 所有交互必须支持键盘操作、可见焦点和 `prefers-reduced-motion`。

## 禁用项

- 禁止同层级混用多套字号、颜色、圆角和按钮高度。
- 禁止通过默认值、假数据或隐藏错误维持表面完整。
- 禁止增加未接后端的假按钮、假入口和不可执行状态。
- 禁止把历史前端、构建产物、源码压缩包或验收临时文件作为第二套运行时入口保留。
